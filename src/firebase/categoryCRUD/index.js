const categoryCRUD = ()=>{

    // What operations should happen when a category is being created
    const BudgetRef = firestore().collection('BudgetRef').doc(email);

    const createCategory = ()=>{

        // Add category to allCategoryObj in budget collection
        // Add category to Budget collection
        // Add category to Expense collection


    }

}